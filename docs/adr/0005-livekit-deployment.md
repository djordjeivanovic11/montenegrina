# ADR 0005: LiveKit deployment

Status: accepted.

Run LiveKit Server locally for credential-free WebRTC tests. Use LiveKit Cloud
for staging media and SIP while deploying custom Python agents on ECS. Operating
a public media/SIP edge is not an MVO differentiator.

